<aside ...attributes>
  <Button @href={{@podcast.rss-url}} @new-tab={{true}} @theme="primary" @material-icon="rss_feed">
    <span class="label">RSS-feed</span>
  </Button>

  {{#each @podcast.links as |link|}}
    <Button @href={{link.url}} @new-tab={{true}} @theme={{link.theme}}>
      {{#if link.custom-icon}}
        <img src={{link.custom-icon}} alt="" class="icon" />
      {{else if link.faIcon}}
        <FaIcon @icon={{link.faIcon}} class="icon" />
      {{/if}}
      <span class="label">{{link.label}}</span>
    </Button>
  {{/each}}

  <audio src="/airhorn.opus" preload="auto" {{on-audio-insert this.setAirhornElement}}></audio>

  <div class="text-center small attribution">
    Webbsidan tillverkades av
    <a
      href="https://robert.huseli.us/"
      class="bold"
      target="_blank"
      rel="noopener noreferrer"
      {{on "mouseenter" this.onMouseEnterRobert}}
    >Robert</a>
  </div>
  {{#if this.showFlash}}
    <div class="flash-overlay" {{on "animationend" this.onFlashAnimationEnd}}></div>
  {{/if}}

  {{yield}}
</aside>
